import React from 'react';
import { Tooltip } from '../TooltipOg'
export default {
title: 'Tooltip - a customizable react component',
component:  Tooltip,
};

const Template = () => <Tooltip/>;
export const Default = Template.bind({});
